rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura/escritura sin autenticación por ahora (cambiar en producción)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

